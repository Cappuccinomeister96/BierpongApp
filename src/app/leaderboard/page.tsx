import { PublicLeaderboard } from "@/components/PublicLeaderboard";

export const metadata = { title: "Leaderboard – Bierpong" };

export default function LeaderboardPage() {
  return <PublicLeaderboard />;
}
